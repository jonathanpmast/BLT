

var nav = function () {
    function to(url) {
        history.pushState(null, null, url);
    };

    function back(num) {
        history.back
    };

    function forward(num) {

    };
}();

module.exports = nav;